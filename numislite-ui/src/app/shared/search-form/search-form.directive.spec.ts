import { SearchFormDirective } from './search-form.directive';

describe('SearchFormDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchFormDirective();
    expect(directive).toBeTruthy();
  });
});
